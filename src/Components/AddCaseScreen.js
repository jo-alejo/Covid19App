import React, {Component} from 'react';
import {Container, Text} from 'native-base';

class AddCaseScreen extends Component {
  render() {
    return (
      <Container>
        <Text> Here we're adding </Text>
      </Container>
    );
  }
}

export default AddCaseScreen;
